#include "minirt.h"
#include "minirt_cam.h"
static void	minirt

t_minirt_screen	*t_minirt_raytrace(t_minirt *minirt, size_t pos)
{
	int				x;
	int				y;	

	x = 0;
	while (t_minirt_cam_getpixel(minirt->objs[type][pos]->cam, x, y))

	return (ret);
}
